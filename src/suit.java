import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({
  lab1.test.newtexttest1.class,
  lab1.test.newtexttest2.class,
  lab1.test.newtexttest3.class,
})

public class suit {
  // the class remains empty,
  // used only as a holder for the above annotations
}
